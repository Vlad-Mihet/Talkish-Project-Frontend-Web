export enum Paths {
  BLOGS = 'blogs',
  BLOG = '/:blogId',
  AUTHOR = '/:authorId',
  TOPICS = 'topics',
  TOPIC = '/:topicId',
  ROOT = '/',
  PAGE_NOT_FOUND = '*',
  WRITE_STORY = 'new-story',
}

export enum UsefulLinks {
  SUPPORT = '/support',
  STATUS = '/status',
  CAREERS = '/careers',
  PRIVACY = '/privacy',
  TERMS = '/terms',
  WRITERS = '/writers',
}
